DEFAULT_DIGITAL_TWIN = {
    "policyId": "org.acme:my-policy",  # Default policy ID
    "attributes": {
        "location": "Germany"  # Default location
    },
    "features": {
        "network": {
            "properties": {
                "neighbors": [],
                "hl_nt": value,
                "tc_int": value,  # Default neighbors (empty list)
                "error": None      # Default error state (None)
            }
        }
    }
}
